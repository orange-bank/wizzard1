apiVersion: v1
kind: Service
metadata:
  name: {{ include "orange-bank.fullname" . }}
  labels:
    {{- include "orange-bank.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "orange-bank.selectorLabels" . | nindent 4 }}
