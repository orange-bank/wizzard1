import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Header } from "@/components/Header";
import { Footer } from "@/components/Footer";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: {
    template: "%s | Orange Bank",
    default: "Orange Bank – Banking that works for you",
  },
  description:
    "Orange Bank – personal banking, mortgages, loans, savings, and current accounts.",
  metadataBase: new URL("https://www.orangebank.ie"),
  openGraph: {
    type: "website",
    locale: "en_IE",
    siteName: "Orange Bank",
  },
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
